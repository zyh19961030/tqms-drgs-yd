<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qu.modules.web.mapper.DynamicTableMapper">

    <update id="createDynamicTable" parameterType="java.lang.String">
        ${sql}
    </update>

    <insert id="insertDynamicTable" parameterType="java.lang.String">
        ${sql}
    </insert>

    <select id="selectDynamicTable" parameterType="java.lang.String" resultType="java.lang.String">
        ${sql}
    </select>

    <update id="updateDynamicTable" parameterType="java.lang.String">
        ${sql}
    </update>

    <select id="countDynamicTable" parameterType="java.lang.String" resultType="java.lang.Long">
        ${sql}
    </select>

    <select id="selectDynamicTableColumn" parameterType="java.lang.String" resultType="map">
        ${sql}
    </select>

    <select id="selectDynamicTableColumnList" parameterType="java.lang.String" resultType="map">
        ${sql}
    </select>

    <select id="selectViewName" parameterType="java.lang.String" resultType="com.qu.modules.web.vo.ViewNameVo">
        ${sql}
    </select>

</mapper>